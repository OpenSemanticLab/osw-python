# Coverage starts at the legacy baseline (~22 %), so statuses are
# informational for now: reports and PR comments without failing CI.
# Tighten targets as coverage grows.
coverage:
  range: 20..80
  round: down
  precision: 1
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
